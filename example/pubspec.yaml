name: flutter_wear_os_connectivity_example
description: Demonstrates how to use the flutter_wear_os_connectivity plugin.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=2.17.6 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  image_picker: ^0.8.6
  flutter_wear_os_connectivity:
    path: ../
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true